package ${classPackage}.constant;
/**
 * @author ${System.getProperty('user.name')}
 * Created: ${new Date()}
 */
public interface CookieConstant {
    String TOKEN = "token";

    String OPENID = "openid";

    /**
     * 过期时间(单位:s)
     */
    Integer expire = 7200;
}
